apiVersion: v1
kind: Service
metadata:
    name: salmafennanlearningmanagementsystem
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: salmafennanlearningmanagementsystem
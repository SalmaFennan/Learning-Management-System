---
# Secrets pour l'authentification (lms-auth)
apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  namespace: lms-auth
type: Opaque
data:
  cloudinary-api-key: "257972227838722"    # Remplacez par votre clé encodée en base64
  cloudinary-api-secret: "FLwCX11V2yZvfG-5BSGtdT8-9CM" # Remplacez par votre secret encodé en base64
  cloudinary-cloud-name: "dqpzmzdjc" # Remplacez par votre cloud name encodé en base64
  cors-origins: "http://localhost:3000,http://localhost:3001-test"               # Remplacez par vos origines CORS encodées en base64
  email-from: "noreply@testdomain.com"             # Remplacez par l'email encodé en base64
  jwt-expiry: "24H"                   # Remplacez par la durée d'expiration encodée en base64
  jwt-secret: "AZcUY56F0KwSMlx8I0bZ6/3exY+Y9T89YTalVeUztyE="                   # Remplacez par votre JWT secret encodé en base64
  smtp-host: "smtp.gmail.com-test"                     # Remplacez par l'hôte SMTP encodé en base64
  smtp-password: "X7k9pL8qW2mN4vR5tY6uJ8hK9mL0pQ1r"             # Remplacez par le mot de passe SMTP encodé en base64
  smtp-port: "587"                     # Remplacez par le port SMTP encodé en base64
  smtp-username: "test-email@gmail.com"             # Remplacez par le nom d'utilisateur SMTP encodé en base64

---
# Secrets pour la base de données des utilisateurs (lms-user)
apiVersion: v1
kind: Secret
metadata:
  name: db-user-secrets
  namespace: lms-user
type: Opaque
data:
  mongo-uri: "mongodb://mongo:27017/lms-auth-test"                     # Remplacez par votre URI MongoDB encodée en base64
  node-env: "development"                       # Remplacez par l'environnement Node encodé en base64
  port: "5001"                               # Remplacez par le port encodé en base64

---
# Secrets pour la base de données des cours (lms-course)
apiVersion: v1
kind: Secret
metadata:
  name: db-course-secrets
  namespace: lms-course
type: Opaque
data:
  mongo-uri: "mongodb://mongo:27017/lms-auth-test"                     # Remplacez par votre URI MongoDB encodée en base64
  node-env: "development"                       # Remplacez par l'environnement Node encodé en base64
  port: "5001"                               # Remplacez par le port encodé en base64

---
# Secrets pour la base de données des paiements (lms-payment)
apiVersion: v1
kind: Secret
metadata:
  name: db-payment-secrets
  namespace: lms-payment
type: Opaque
data:
  mongo-uri: "mongodb://mongo:27017/lms-auth-test"                     # Remplacez par votre URI MongoDB encodée en base64
  node-env: "development"                       # Remplacez par l'environnement Node encodé en base64
  port: "5001"                               # Remplacez par le port encodé en base64

---
# Secrets pour les paiements (lms-payment)
apiVersion: v1
kind: Secret
metadata:
  name: payment-secrets
  namespace: lms-payment
type: Opaque
data:
  razorpay-key-id: "rzp_test_X7k9pL8qW2mN4vR5"        # Remplacez par votre clé Razorpay encodée en base64
  razorpay-secret: "X7k9pL8qW2mN4vR5tY6uJ8hK9mL0pQ1r"         # Remplacez par votre secret Razorpay encodé en base64
  rate-limit-max: "100"           # Remplacez par la limite max encodée en base64
  rate-limit-window-ms: "900000" # Remplacez par la fenêtre de temps encodée en base64
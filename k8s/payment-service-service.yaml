# payment-service-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: payment-service-service
  namespace: lms-payment
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3004
  type: ClusterIP